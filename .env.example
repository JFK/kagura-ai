# =============================================================================
# Kagura AI Environment Variables
# =============================================================================
#
# Copy this file to .env and fill in your API keys:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to Git (it's in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# LLM API Keys (at least one required)
# -----------------------------------------------------------------------------

# OpenAI (GPT-4, GPT-5, etc.)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Google Gemini
# Get from: https://ai.google.dev/
GOOGLE_API_KEY=AIza...

# Anthropic Claude
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# Optional: Web Search
# -----------------------------------------------------------------------------

# Brave Search API (recommended for web search)
# Get from: https://brave.com/search/api/ (free tier: 2,000 queries/month)
BRAVE_SEARCH_API_KEY=BSA...

# -----------------------------------------------------------------------------
# Backend Configuration (Issue #668)
# -----------------------------------------------------------------------------

# PostgreSQL Database (recommended for production)
# DATABASE_URL=postgresql://user:pass@localhost:5432/kagura
# Or SQLite (default for development):
# DATABASE_URL=sqlite:///~/.kagura/memory.db

# Redis Cache (recommended for production)
# REDIS_URL=redis://localhost:6379
# CACHE_BACKEND=redis  # Options: memory, redis

# Qdrant Vector Database (recommended for production)
# QDRANT_URL=http://localhost:6333
# VECTOR_BACKEND=qdrant  # Options: chromadb, qdrant

# Backend Selection (default: sqlite + memory cache + chromadb)
# GRAPH_BACKEND=postgres  # Options: networkx, postgres
# PERSISTENT_BACKEND=postgres  # Options: sqlite, postgres
# CACHE_BACKEND=redis  # Options: memory, redis
# VECTOR_BACKEND=qdrant  # Options: chromadb, qdrant

# -----------------------------------------------------------------------------
# Development
# -----------------------------------------------------------------------------

# Debug mode (enables verbose logging)
# DEBUG=true

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Embeddings Configuration (Cloud Deployment)
# -----------------------------------------------------------------------------

# Embedding provider (openai or sentence-transformers)
# For small VMs (4GB RAM), use OpenAI API:
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small  # Recommended: $0.02/1M tokens, 1536 dims

# For large VMs (8GB+ RAM), you can use local models:
# EMBEDDING_PROVIDER=sentence-transformers
# EMBEDDING_MODEL=intfloat/multilingual-e5-large  # 1024 dims, requires ~2GB RAM

# -----------------------------------------------------------------------------
# Reranking Configuration (Issue #668)
# -----------------------------------------------------------------------------

# Reranking provider (local, cohere, jina)
# RERANKING_PROVIDER=local  # Options: local, cohere, jina

# For local reranking (requires sentence-transformers):
# KAGURA_ENABLE_RERANKING=true

# For Cohere Rerank API:
# RERANKING_PROVIDER=cohere
# COHERE_API_KEY=your-cohere-api-key

# For Jina AI Reranker API:
# RERANKING_PROVIDER=jina
# JINA_API_KEY=your-jina-api-key

# -----------------------------------------------------------------------------
# Remote MCP Server (Issue #668)
# -----------------------------------------------------------------------------

# Enable remote MCP server (HTTP/SSE transport for ChatGPT/Claude)
# MCP_REMOTE_ENABLED=true

# Filter tools by category (optional, comma-separated)
# MCP_ALLOWED_CATEGORIES=memory,coding,search,github

# -----------------------------------------------------------------------------
# Production Deployment (Docker)
# -----------------------------------------------------------------------------

# Domain name (required for HTTPS with Caddy)
# DOMAIN=your-domain.com

# PostgreSQL password (required for production)
# POSTGRES_PASSWORD=change_this_secure_password

# PostgreSQL database and user (optional, defaults: kagura/kagura)
# POSTGRES_DB=kagura
# POSTGRES_USER=kagura

# Require API key for all requests (optional, default: false)
# API_KEY_REQUIRED=false

# CORS origins (comma-separated)
# CORS_ORIGINS=https://chat.openai.com,https://claude.ai
