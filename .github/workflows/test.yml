# File: .github/workflows/test.yml
name: Run tests

on:
  workflow_call: {}
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'tests/**'
      - 'pyproject.toml'
  pull_request:
    paths:
      - 'src/**'
      - 'tests/**'
      - 'pyproject.toml'

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Setup caching
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/uv
            ~/.cache/pip
          key: ${{ runner.os }}-python-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-python-

      - name: Install dependencies
        run: |
          uv sync --frozen --all-extras --dev

      - name: Run tests (agents)
        run: uv run pytest tests/agents/ -v --tb=short
        timeout-minutes: 1

      - name: Run tests (cli)
        run: uv run pytest tests/cli/ -v --tb=short
        timeout-minutes: 1

      - name: Run tests (core/agent)
        run: uv run pytest tests/core/test_agent_implementation.py tests/core/test_decorators.py -v --tb=short
        timeout-minutes: 1

      - name: Run tests (core/executor part 1)
        run: uv run pytest tests/core/test_executor.py -k "test_simple or test_math or test_multiple or test_list or test_string or test_dict or test_stdout or test_multiple_prints" -v --tb=short
        timeout-minutes: 1

      - name: Run tests (core/executor part 2)
        run: uv run pytest tests/core/test_executor.py -k "test_disallowed or test_allowed" -v --tb=short
        timeout-minutes: 1

      - name: Run tests (core/executor part 3)
        run: uv run pytest tests/core/test_executor.py -k "test_syntax or test_runtime or test_name or test_type" -v --tb=short
        timeout-minutes: 1

      - name: Run tests (core/executor timeout 1)
        run: uv run pytest tests/core/test_executor.py::test_timeout -v --tb=short
        timeout-minutes: 1

      # Skipping test_infinite_loop_timeout - causes pytest-asyncio cleanup to hang

      - name: Run tests (core/parser)
        run: uv run pytest tests/core/test_parser.py -v --tb=short
        timeout-minutes: 1

      - name: Run tests (core/prompt)
        run: uv run pytest tests/core/test_prompt.py -v --tb=short
        timeout-minutes: 1

      - name: Run tests (integration)
        run: uv run pytest tests/integration/ -v --tb=short
        timeout-minutes: 1

      - name: Run tests (version)
        run: uv run pytest tests/test_version.py -v --tb=short
        timeout-minutes: 1
